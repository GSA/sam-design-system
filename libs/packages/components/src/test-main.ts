// Placeholder entry point for the `@angular/build:application` build-test target.
// The `@angular/build:unit-test` builder overrides entryPoints with the
// discovered spec files, so this file is only required to satisfy the
// application builder's schema and is never used as an actual bundle entry.
export {};
