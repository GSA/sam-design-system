export class SdsTableSettings {
  /**
   * table without border
   */
  borderless?: boolean;
  /*
   * sortable table
   */
  sort?: boolean;
  /*
   * Include sticky header row
   */
  stickyHeader?: boolean;
}
